import React from "react";
import ReactDOM from "react-dom/client";
import FacialTerminal from "./pages/FacialTerminal";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FacialTerminal />
  </React.StrictMode>,
);
